//all options to filter the task list
export const enum FilterOptions {
  All = "All",
  Completed = "Completed",
  InCompleted = "InCompleted",
}

//all options to sort the task list
export const enum SortOptions {
  All = "All",
  Name = "Name",
  Date = "Date",
}
